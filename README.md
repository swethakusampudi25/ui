# ui
React UI Module for Early Eco application
# Auth UI

## Setup

```bash
npm install
cp .env.example .env
npm run dev
```

Environment variable:

- `BE_BASE_URL` - backend base URL (for example `http://127.0.0.1:8000`)